{
    entry = {
	bundle = TimePasscodePreferences;
	cell = PSLinkCell;
	detail = TimePasscodePreferencesListController;
	icon = TimePasscodePreferences.png;
	isController = 1;
	label = TimePasscode;
    };
}
